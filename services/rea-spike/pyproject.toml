[project]
name = "rea-spike"
version = "0.0.1"
description = "Throwaway Phase 0 spike for realestate.com.au scraping via camoufox."
requires-python = ">=3.11"
dependencies = [
    "camoufox[geoip]>=0.4.0",
]
