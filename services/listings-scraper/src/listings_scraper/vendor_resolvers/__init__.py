"""Vendor suburb resolver abstractions and implementations.

Sub-packages:
- `base` — ResolvedSuburb model, VendorSuburbResolver Protocol
- `oth/` — OTH autocomplete-based resolver (was `suburb_resolver/`)
"""
